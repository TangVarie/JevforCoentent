TYPESAFE_API_KEY=            # Jev 密钥，只放服务端
JEV_MODEL=                   # 留空 = 用题库里固定的 jev-1.13.0
JUDGE_API_KEY=               # HTTP 调用方要带的 X-Judge-Key
SUPABASE_URL=                # 直接写账本时用（可空，只出 SQL）
SUPABASE_SERVICE_KEY=
SOCIALDATAX_API_KEY=         # 外部语料
JUDGE_MOCK=0
