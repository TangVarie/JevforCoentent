# 拍板记录（2026-09-23）

> 依据：truth-vault docs/31（Jev for BYWOOD content）§9.2 的九件事，加上当天追问的三件（生产回路怎么落、飞轮近亲繁殖、评论怎么写）。
> Ziao 定了两条：服务不放 TV、闸一不补；其余按 Claude 判断。

| # | 事 | 定 | 为什么 |
|---|---|---|---|
| 1 | 服务归属 | **独立仓库（本仓，暂名 judge），Railway 单独一个服务；三仓只放薄客户端** | 「深度集成」靠三样东西：共用一张账本（`note_feature_answers`）、共用一套题库纪律、每仓一个像 `librarian_client.py` 那样的薄客户端；不靠代码放在同一个仓里。TV 已有 worker / librarian / onboarder / dashboard 四个服务，`ci.yml` 离 505,000 字节的棘轮上限只剩约 1.3 KB（D-075），再塞一个服务就是在往最挤的地方加东西。题库跨三仓（评论题库 TV 用、三省六部也用），放任何一仓都要另外两仓来拉。 |
| 2 | 闸一人工锚 | **不补（Ziao 拍板）** | 替代物：① Jev 与现行抽取器的一致率（50 篇 90.7%，两条 Jev 路径互相 95.5%）；② 歧义队列（12% 的格子自报把握不够，和 D-081 的 12% 一致）；③ 闸二 / 闸三——特征有没有用，最终由平台结果说话，不由人怎么看说话。代价写明：fq 里最难的几题（转折、点名读者群体、叙述者身份、开头类型、产品角色）Jev 与 Opus 的分歧是系统性的（各自一致、互相不一致），谁对没有裁判；处理办法是这几题的定义在闸二之前收紧一轮，歧义格进复核，而不是补人工锚。 |
| 3 | 账本扩展 | **subject_type 加 comment / ssll_sample / external_note（migrations/notes_v1_17）；prob 统一为「所选答案的概率」；歧义不落库，读取时按题库阈值算** | 一张表管所有判定，闸二的 SQL 不用改。歧义规则属于题库版本，不属于数据。 |
| 4 | 写作台入库判定 | **挂 `commit_drafts` 内部，advisory，超时 8 秒即跳过，答案进账本；影子期只记不拦；写手侧同时提供 MCP 工具（judge_draft / repair_plan_for）** | 写手「抽完就散场」（D-071），入库是唯一必经的一步；MCP 工具让写手在会话里就能拿到修改单，不用等 deskcore 改代码。暗题：人感题库的 1/3 不写进任何 prompt，每季度换。 |
| 5 | 三省六部 | **二审先影子跑（记不分流），采样正文落库后再判；persona 第三路后做** | 改 `PIPELINE_STAGE_ORDER` 和 resume 逻辑的风险要三省六部维护者自己担，本仓只给客户端和题库。 |
| 6 | 评论题库两侧 | **运营侧按表里现有六值写（co-v0.1 草案）；读者侧 7 题金标准扩到 100 条，由 TV 侧两人对着定义标** | 33 条真实运营评论实跑：`蓝词植入` 一次没判出来——它其实是代码特征（`contains_blue_keyword` 列已有），不该让 Jev 判；勘误时把这条从题里拿掉或改定义。 |
| 7 | 数据出境 | **公开笔记直接跑；未发布稿默认只跑通用层 + 平台层；处方药项目的未发布稿不跑，直到合同确认** | docs/30 §5 的风险原样保留。 |
| 8 | 素人初稿入口 | **后做，接同一个 `/judge`** | 链路不在三仓里。 |
| 9 | 题库维护 | **fq = TV；人感 = 写作台 + human-writing 维护者；评论 = TV；平台 = 合规；项目层 = 写作台按 brief 编** | 改题流程：定义 → 金标准 → 重跑 → 升版本 → 记一笔。 |
| 10 | 生产回路（追问一） | **`judge/loop.py` 就是实现：写前 `compile_project_bank`、写中 `best_of_k`、写后 `judge_draft → repair_plan → repair`、整条 `produce`；写手侧 MCP、服务器侧 HTTP 两个入口** | 两篇真实笔记实跑：每篇 10–12 次调用、4.5 秒，抓到「效果是真的绝…立马就压住」判疗效暗示 0.68、「省下的烟钱都够给老婆买个包」判省钱 0.77，都带证据句；段级分布给出「无产品意图段 83%、产品第一次出现在第 4 段、有摩擦」。生成端是可插拔的适配器，接三省六部那个中转站即可。 |
| 11 | 飞轮近亲繁殖（追问二） | **加第八个位置：外部语料。供应商 TikHub（每次请求 0.01 美元，10 次/秒），GitHub Actions 每周一跑；每品类每次最多 40 篇全文、每月 160 篇，一次运行硬上限 4 美元；搜索页先过分诊四题（同品类、普通用户视角、像不像广告、有没有产品）再花钱取全文；同一套 fq 题库打标，落 external_notes 表 + 账本 external_note；参考分布由视图 `v_external_reference` 直接算；不进 v_l2_labels** | 飞轮现在只有结果的外部性（平台数据），没有样本的外部性（别人写的内容），闸二学到的上限就是自家最好的那篇。外部语料给「人感」一个不是自家定义的参考分布，也给闸二一个「自家显著的特征在外部是否同向」的复核。15% 随机 batch 管的是生成端的 Goodhart，外部语料管的是数据端的近亲繁殖，两个都要。SocialDataX 通道三省六部和 Ziao 自己都用过，新的是打标和入账本。 |
| 12 | 评论怎么写（追问三） | **加 `judge/comments.py`：评论位 → best-of-k → 修改单 → 成组判 → 换位，与稿子的回路同构；题库升 cb-v0.4 / tb-v0.3 只加占位符（途鸽用例逐字等同旧版，金标准不重跑）；MCP 加 `comment_repair_plan_for` / `judge_thread`；默认组不含背书位** | 之前只有「判一批候选」没有「从要什么到出来什么」；机制和 docs/01 一页说清。背书体在题库里就是高风险复核项，回路只换不修——途鸽实跑五条背书体全判「像安排的」，修得再像也是在读者最先识破的那一类上花钱。 |

## 顺手查到、要回三仓修的

- TV：闸一名单分隔符不一致（`build_gate1_human_sheets.py:329` 写 `|`，`ingest_gate1_answers.py:147` 读 `,`）；`prob` 口径不统一；`annotate_feature_pass` 续跑判据按 `llm:%`；`sync_comments_from_raw_extra` 的 Pattern A 只按行首切，「 7. … 8. …」写在同一行的评论被并成一条（33 条样本里有 4 条）。
- 三省六部：`_validate_prompt_cell` 的 11 条 ai_cliches 和 `prose_gate` 的 15 条是两份；`_prose_soft_flags` 写了没人读。
- 写作台：`angle_ledger` 不记结果，发牌加权前要一个 `consumed_version_id → tv_note_links → notes.tier` 的视图。
